{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SSK",
  "type": "object",
  "required": ["pieces"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+$"
    },
    "pieces": {
      "type": "array",
      "items": { "$ref": "#/$defs/piece" }
    },
    "properties": { "type": "object" }
  },
  "$defs": {
    "piece": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id":       { "type": "integer", "minimum": 0 },
        "from":     { "type": "integer", "minimum": 0 },
        "points":   { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/point" } },
        "rotation": { "$ref": "#/$defs/vector3" },
        "size":     { "$ref": "#/$defs/vector3" },
        "shape":    { "type": "string", "enum": ["circle", "ngon"] },
        "sides":    { "type": "integer", "minimum": 3 },
        "mode":     { "type": "string", "enum": ["add", "subtract", "intersect"] },
        "affects":  { "type": "array", "uniqueItems": true, "items": { "type": "integer", "minimum": 0 } },
        "properties": { "type": "object" }
      },
      "required": ["id"],
      "if": {
        "not": { "required": ["from"] }
      },
      "then": {
        "required": ["id", "points", "rotation", "size", "shape"]
      }
    },
    "point": {
      "type": "object",
      "required": ["x", "y", "z"],
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" },
        "curve_in":  { "$ref": "#/$defs/vector3" },
        "curve_out": { "$ref": "#/$defs/vector3" },
        "size": { "$ref": "#/$defs/vector3" },
        "rotation": { "$ref": "#/$defs/vector3" },
        "transition_in": { "$ref": "#/$defs/vector2" },
        "transition_out": { "$ref": "#/$defs/vector2" }
      }
    },
    "vector2": {
      "type": "object",
      "required": ["x", "y"],
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" }
      }
    },
    "vector3": {
      "type": "object",
      "required": ["x", "y", "z"],
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "z": { "type": "number" }
      }
    }
  }
}
